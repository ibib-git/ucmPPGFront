import { MembreActifPipe } from './membre-actif.pipe';

describe('MembreActifPipe', () => {
  it('create an instance', () => {
    const pipe = new MembreActifPipe();
    expect(pipe).toBeTruthy();
  });
});
