import { InputErrorsPipe } from './input-errors.pipe';

describe('InputErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new InputErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
