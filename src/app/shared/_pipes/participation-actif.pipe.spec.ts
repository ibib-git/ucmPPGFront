import { ParticipationActifPipe } from './participation-actif.pipe';

describe('ParticipationActifPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipationActifPipe();
    expect(pipe).toBeTruthy();
  });
});
