import { OrdreEtapePipe } from './ordre-etape.pipe';

describe('OrdreEtapePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdreEtapePipe();
    expect(pipe).toBeTruthy();
  });
});
