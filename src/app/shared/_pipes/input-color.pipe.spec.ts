import { InputColorPipe } from './input-color.pipe';

describe('InputColorPipe', () => {
  it('create an instance', () => {
    const pipe = new InputColorPipe();
    expect(pipe).toBeTruthy();
  });
});
